#include <bits/stdc++.h>
#define ll long long int
#define fastIO ios_base::sync_with_stdio(false); cin.tie(NULL);
#define N 9

using namespace std;

int main() {
    int grid[N][N];
    cout << "Enter the initial sudoku grid (0 for blank):" << endl << endl;
    for(int i = 0; i < N; i++)
        for(int j = 0; j < N; j++) 
            cin >> grid[i][j];
    
    
}